machine:
  java:
    version: oraclejdk8
  node:
    version: 6.2.0

general:
  branches:
    ignore:
      - gh-pages

dependencies:
  override:
    - npm install

test:
  override:
    - npm run testonly:
        parallel: true
