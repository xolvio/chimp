import { MutationResolvers } from "{{generatedPrefix}}/graphql/types";

export const {{mutationName}}Mutation: MutationResolvers["{{mutationName}}"] = (parent, args, context) => { throw new Error("not implemented yet"); }
