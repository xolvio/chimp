import { QueryResolvers } from "{{generatedPrefix}}/graphql/types";

export const {{queryName}}Query: QueryResolvers["{{queryName}}"] = (parent, args, context) => { throw new Error("not implemented yet"); }

